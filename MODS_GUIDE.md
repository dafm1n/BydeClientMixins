мм# BydeClient - PvP Клиент с Модами

Это Minecraft 1.8.9 клиент с поддержкой модов для PvP. Включает три основных мода: **Keystrokes**, **AutoGG** и **FPS Boost**.

## 📋 Структура проекта

```
src/main/java/instance/bydeclient/dev/BydeClient/mixin/
├── modules/
│   ├── Module.java              # Базовый класс для всех модов
│   ├── KeystrokesModule.java    # Мод отображения нажатых клавиш
│   ├── AutoGGModule.java        # Мод автоматического "gg"
│   ├── FPSBoostModule.java      # Мод оптимизации FPS
│   └── ModuleManager.java       # Менеджер для управления модами
├── mixins/
│   ├── BydeClientTweaker.java   # Главный tweaker
│   └── Mixin/
│       ├── MixinRender.java     # Рендер ESP
│       ├── MixinGuiIngame.java  # Рендер GUI модов
│       └── MixinEntityPlayerSP.java # События игрок��
```

## 🎮 Модули

### 1. **Keystrokes** (Отображение нажатых клавиш)
- Показывает нажатые клавиши WASD и SPACE на экране
- Зелёный цвет = клавиша нажата
- Серый цвет = клавиша не нажата
- Расположение: правый нижний угол экрана

**Использование:**
```java
ModuleManager manager = ModuleManager.getInstance();
manager.enableModule("Keystrokes");
manager.disableModule("Keystrokes");
```

### 2. **AutoGG** (Автоматическое "gg")
- Автоматически пишет "gg" после окончания игры
- Срабатывает при смерти игрока
- Имеет задержку 1 секунда между сообщениями

**Использование:**
```java
ModuleManager manager = ModuleManager.getInstance();
manager.enableModule("AutoGG");
```

### 3. **FPS Boost** (Оптимизация производительности)
- Отключает VSync
- Минимизирует частицы
- Уменьшает дальность рендера до 8 чанков
- Отключает облака и fancy graphics
- Восстанавливает оригинальные настройки при отключении

**Использование:**
```java
ModuleManager manager = ModuleManager.getInstance();
manager.enableModule("FPS Boost");

// Получить информацию об оптимизациях
String info = manager.getFPSBoostModule().getOptimizationInfo();
System.out.println(info);
```

## 🔧 Как использовать ModuleManager

```java
// Получить экземпляр менеджера
ModuleManager manager = ModuleManager.getInstance();

// Включить модуль
manager.enableModule("Keystrokes");

// Отключить модуль
manager.disableModule("Keystrokes");

// Переключить модуль
manager.toggleModule("Keystrokes");

// Получить модуль
Module module = manager.getModule("Keystrokes");

// Получить все модули
List<Module> modules = manager.getModules();

// Отключить все модули
manager.disableAll();

// Вывести статус всех модулей
manager.printModuleStatus();
```

## 📝 Создание собственного мода

1. Создайте класс, наследующий `Module`:

```java
public class MyModule extends Module {
    public MyModule() {
        super("MyModule", "Описание моего мода");
    }

    @Override
    public void onEnable() {
        System.out.println("Мод включен");
    }

    @Override
    public void onDisable() {
        System.out.println("Мод отключен");
    }

    @Override
    public void onUpdate() {
        // Логика обновления каждый тик
    }
}
```

2. Добавьте в `ModuleManager.initializeModules()`:

```java
MyModule myModule = new MyModule();
addModule(myModule);
```

## 🏗️ Сборка проекта

```bash
# Сборка JAR файла
./gradlew build

# Запуск клиента
./gradlew runClient
```

## 📦 Зависимости

- Minecraft 1.8.9
- Forge (ForgeGradle 2.1)
- Mixin 0.7.11
- Java 8

## 🎯 Особенности

- ✅ Модульная архитектура
- ✅ Легко добавлять новые модули
- ✅ Автоматическое управление состоянием модулей
- ✅ Поддержка Mixins для глубокой интеграции
- ✅ Оптимизированный код для PvP

## 🚀 Планы развития

- [ ] Добавить GUI для управления модами
- [ ] Реализовать систему горячих клавиш
- [ ] Добавить мод Combat Assistant
- [ ] Реализовать систему конфигурации
- [ ] Добавить мод Reach Display
- [ ] Реализовать мод Hit Effect

## 📄 Лицензия

Этот проект создан в образовательных целях.

---

**Автор:** BydeClient Dev Team  
**Версия:** 1.0  
**Minecraft:** 1.8.9
